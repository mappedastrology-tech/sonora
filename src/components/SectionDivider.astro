---
/**
 * A single 32px arch, centred, between major sections.
 */
import Arch from './Arch.astro';
---

<div class="divider">
  <Arch variant="divider" size={32} />
</div>

<style>
  .divider {
    padding-block: clamp(48px, 6vw, 72px);
  }
</style>
